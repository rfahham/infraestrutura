# Start

Inicializa uma instância que estava stopada

```bash
gcloud compute instances start <Nome da Instância> --zone=<Nome da zona> 
```

Exemplo:
gcloud compute instances start performance-testing --zone=us-east1-b